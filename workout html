<!doctype html>
<html>
<head>
    <title>{{ category }} Workout</title>
</head>
<body>
    <h1>{{ category }} Exercises</h1>
    <ul>
        {% for exercise in exercises %}
            <li>{{ exercise }}</li>
        {% endfor %}
    </ul>
    <a href="/">Back</a>
</body>
</html>
